{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bass.serviceAccountName" . }}
  labels:
    {{- include "bass.labels" . | nindent 4 }}
{{- end }}
